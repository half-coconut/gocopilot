apiVersion: v1
kind: Service
metadata:
  name: testengine-live-redis
spec:
  selector:
    app: testengine-live-redis
  ports:
    - protocol: TCP
#      port: 6380
      port: 11479
#      Redis 默认端口
      targetPort: 6379
      nodePort: 30003
  type: NodePort